/**
 * Scroll controller hook — use inside MotionProvider.
 * Returns Lenis-driven scroll progress and ref for section triggers.
 */
export { useScrollController } from "@/lib/motion/scroll-context";
