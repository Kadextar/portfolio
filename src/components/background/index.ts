/**
 * Background layers: WebGL (optional) + dynamic gradient.
 * Compose in ClientProviders: WebGLBackground (back) then DynamicBackground (front).
 */
export { DynamicBackground } from "@/components/DynamicBackground";
export { WebGLBackground } from "./WebGLBackground";
