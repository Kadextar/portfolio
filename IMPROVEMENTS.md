# Улучшения портфолио — чек-лист

Что уже сделано и что можно добавить с точки зрения продакшн-уровня.

---

## ✅ Уже сделано

- **SEO:** metadata (title, description, canonical, alternates), JSON-LD Person, OG image, sitemap (все локали + /info, /contact), robots.txt
- **Локализация:** next-intl, en/ru/uz, сохранение языка в localStorage, 404 и error на всех языках
- **Доступность:** skip link, focus-visible на навигации и футере, reduce-motion, семантика (nav, main, headings)
- **Производительность:** ленивая загрузка WebGL-фона, динамический импорт
- **Аналитика:** Plausible (опционально), UTM-ссылки
- **Безопасность:** заголовки X-Frame-Options, X-Content-Type-Options, Referrer-Policy
- **Ошибки:** error boundary (error.tsx) с «Попробовать снова» и ссылкой на главную
- **Печать:** print-стили, скрытие навбара/футера при печати
- **Контент:** кейсы Schedy/Kadextar, лайтбокс для скриншотов, резюме PDF

---

## 🔲 Что можно добавить (по приоритету)

### Высокий приоритет — ✅ сделано

1. ~~Обновить Next.js~~ — обновлено до 14.2.35.
2. ~~Тесты~~ — Jest + RTL, тесты на Button и Footer.
3. ~~Валидация окружения~~ — скрипт `scripts/validate-env.mjs` перед сборкой.

### Средний приоритет — ✅ сделано

4. ~~Картинки Schedy через next/image~~ — в Projects используется `next/image` для сетки и лайтбокса.
5. ~~Loading UI~~ — добавлены `loading.tsx` в `[locale]`, `[locale]/info`, `[locale]/contact`.
6. ~~Структурированные данные~~ — на странице контактов добавлен JSON-LD `ContactPage` с `mainEntity` Person.
7. ~~Шрифты~~ — у шрифтов уже `display: "swap"` в layout; в `globals.css` добавлен fallback `-apple-system`.

### Низкий приоритет — ✅ сделано

8. ~~PWA~~ — `public/manifest.json`, `public/sw.js`, регистрация SW в `PwaSwRegister`, в metadata указан `manifest`.
9. ~~Мониторинг ошибок~~ — подключён `@sentry/nextjs`, конфиг клиента, `instrumentation.ts` (server/edge), `global-error.tsx`.
10. ~~CI/CD~~ — `.github/workflows/ci.yml`: lint, test, build на push/PR в main/master.
11. ~~Кэш~~ — в `next.config.js` добавлены заголовки Cache-Control для `/_next/static/*`, `/schedy/*`, `/favicon.svg`.
12. ~~Документация~~ — обновлён README (запуск, скрипты, env, деплой, структура), расширен `.env.example` (Plausible, Sentry).

---

## Дальнейшие улучшения (по желанию)

- **Контент:** добавить ещё проекты или обновлять кейсы Schedy/Kadextar (новые скрины, ссылка на App Store когда выйдет).
- **Блог/статьи:** если захочешь вести заметки или кейсы — отдельный раздел или страницы под посты (можно на MDX или CMS).
- **Тёмная/светлая тема:** сейчас только тёмная; при желании — переключатель и сохранение в localStorage.
- **Ещё тесты:** e2e (Playwright) — открытие страниц, смена языка, клики по навигации; или тесты на другие компоненты.
- **Обновление стека:** позже — Next.js 15, React 19, когда next-intl и зависимости будут стабильны.
- **Ссылки на проекты:** кнопки «Открыть Schedy» (App Store), «Канал Kadextar» (YouTube/Telegram/Instagram) прямо в карточках проектов.

---

## Ультра-профессиональные апгрейды

Идеи уровня серьёзных продакшен-проектов и сильного портфолио.

### Производительность и метрики

| # | Улучшение | Зачем |
|---|-----------|--------|
| 1 | **Lighthouse CI** в GitHub Actions | Автоматические проверки LCP, FID, CLS и доступности при каждом PR; падение билда при деградации. |
| 2 | **Bundle analyzer** (`@next/bundle-analyzer`) | Понимание размера бандла, поиск тяжёлых зависимостей (например Framer Motion), решение про code-splitting. |
| 3 | **Resource hints** | `preconnect` для домена Plausible и Google Fonts в layout — быстрее первый запрос. |
| 4 | **Priority для LCP** | У ключевого изображения (если есть) — `fetchPriority="high"` и `priority` у `next/image`. |

### Безопасность

| # | Улучшение | Зачем |
|---|-----------|--------|
| 5 | **Content-Security-Policy (CSP)** | Строгая политика скриптов/стилей; начать с `Content-Security-Policy-Report-Only` и лог в Sentry. |
| 6 | **Permissions-Policy** | Ограничение камеры, геолокации и т.п. — меньше поверхность атаки. |

### SEO и структурированные данные

| # | Улучшение | Зачем |
|---|-----------|--------|
| 7 | **JSON-LD WebSite** | Схема с `url`, `potentialAction` (SearchAction) — богатые результаты и понимание сайта поисковиками. |
| 8 | **BreadcrumbList** на внутренних страницах | Хлебные крошки в сниппетах (Info, Contact). |
| 9 | **Проверка hreflang** | Убедиться, что next-intl отдаёт все локали в `<link rel="alternate" hreflang="...">`. |

### Доступность (a11y)

| # | Улучшение | Зачем |
|---|-----------|--------|
| 10 | **Focus trap в лайтбоксе** | При открытии модалки фокус остаётся внутри (Tab не уходит на страницу); при закрытии — возврат на кнопку. |
| 11 | **axe-core в CI** | Автоматический прогон axe (например `@axe-core/playwright`) в e2e — ловит типовые a11y-ошибки. |
| 12 | **ARIA live для смены языка** | Объявление для скринридеров типа «Язык изменён на русский». |

### Аналитика и продукт

| # | Улучшение | Зачем |
|---|-----------|--------|
| 13 | **Кастомные события в Plausible** | События: «Resume clicked», «Project opened», «Contact CTA», «Language switched» — видно, что реально используют. |
| 14 | **Цели в Sentry** | Release health, алерты при росте ошибок после деплоя. |

### Качество кода и инфра

| # | Улучшение | Зачем |
|---|-----------|--------|
| 15 | **E2E (Playwright)** | Критичные сценарии: загрузка главной, навигация, смена локали, клик «Резюме», открытие лайтбокса и «Назад». |
| 16 | **Dependabot / Renovate** | Авто-PR с обновлением зависимостей — меньше технического долга и уязвимостей. |
| 17 | **Staging-окружение** | Деплой ветки (например `develop`) на Netlify; тестировать перед мержем в main. |
| 18 | **Health check** | Эндпоинт типа `/api/health` (статус 200 + версия) для мониторинга и Uptime Robot. |

### Контент и UX

| # | Улучшение | Зачем |
|---|-----------|--------|
| 19 | **Явные CTA в карточках** | Кнопки «Скачать Schedy», «Смотреть Kadextar» (ссылки на App Store / YouTube / соцсети). |
| 20 | **Тема светлая/тёмная** | Переключатель + сохранение в localStorage — плюс к удобству и демонстрации навыков. |

---

## Ультра-макс (следующий уровень)

Что ещё можно сделать, чтобы портфолио было на уровне топовых продакшен-проектов.

### Производительность и наблюдаемость

| # | Апгрейд | Зачем |
|---|---------|--------|
| 1 | **Bundle analyzer** | `@next/bundle-analyzer`: скрипт `npm run analyze`, отчёт по размеру чанков и зависимостей (Framer Motion, Three.js и т.д.), решение про code-splitting и lazy. |
| 2 | **LCP priority** | У первого видимого изображения (если появится hero-image) — `priority` и `fetchPriority="high"` в `next/image`. |
| 3 | **Core Web Vitals в Plausible** | Включить отслеживание Web Vitals в Plausible (если есть план) или отправлять метрики в кастомные цели. |

### SEO и разметка

| # | Апгрейд | Зачем |
|---|---------|--------|
| 4 | **BreadcrumbList (JSON-LD)** | На страницах Info и Contact добавить схему BreadcrumbList (Home → Info / Contact) — богатые сниппеты в поиске. |
| 5 | **Проверка hreflang** | Убедиться, что в `<head>` есть `link rel="alternate" hreflang="x-default"` и для en/ru/uz; при необходимости дописать в metadata. |

### Доступность (a11y) в CI

| # | Апгрейд | Зачем |
|---|---------|--------|
| 6 | **axe-core в E2E** | Добавить в Playwright тесты прогон `@axe-core/playwright`: автоматическая проверка a11y (контраст, лейблы, ARIA) при каждом прогоне. |
| 7 | **ARIA live при смене языка** | При переключении локали — объявление для скринридеров (например `aria-live="polite"` + «Язык изменён на русский») в одном месте. |

### Безопасность и релизы

| # | Апгрейд | Зачем |
|---|---------|--------|
| 8 | **CSP без Report-Only** | После проверки отчётов перевести Content-Security-Policy из Report-Only в обычный режим (постепенно, по директивам). |
| 9 | **Sentry Release Health** | Настроить привязку деплоев к релизам в Sentry (source maps, release name из Git) и алерты при росте ошибок после деплоя. |

### Качество и инфра

| # | Апгрейд | Зачем |
|---|---------|--------|
| 10 | **E2E на все локали** | В smoke-тестах добавить прогон ключевых сценариев для `/ru` и `/uz` (или параметризованный тест по локалям). |
| 11 | **Staging branch** | Ветка `develop` с авто-деплоем на Netlify (preview URL); в CI запускать E2E против staging перед мержем в main. |
| 12 | **sharp в production** | Установить `sharp` для оптимизации изображений Next.js в проде — убрать предупреждение и ускорить обработку картинок. |

### Контент и полировка

| # | Апгрейд | Зачем |
|---|---------|--------|
| 13 | **Open Graph per page** | Отдельные `openGraph.images` и при необходимости `twitter:image` для /info и /contact (сейчас общие). |
| 14 | **Микро-анимации и feedback** | Точечные улучшения: состояние loading у кнопок (например «Отправить»), hover/focus на карточках проектов, скелетон при загрузке списка (если появится динамический контент). |

---

## Итог

Базовый и средний уровень закрыты. «Ультра-профессиональные апгрейды» в основном внедрены. Раздел «Ультра-макс» — следующий шаг: bundle analyzer, BreadcrumbList, axe в E2E, CSP в боевой режим, Sentry release health, E2E по локалям, staging, sharp, полировка OG и UX.
