# Ambient audio

Place your ambient track here:

- **File name:** `ambient.mp3`
- **Recommended:** Soft cinematic ambient, seamless loop, ~2–5 min.
- **Format:** MP3 (or update `AMBIENT_SRC` in `AmbientAudio.tsx` to use another format).

The app will lazy-load this file only after the user first clicks the sound toggle.
